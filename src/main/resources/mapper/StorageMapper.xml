<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seu.film.mapper.StorageMapper">
    <!-- int addHistory(History history) -->
    <insert id="addStorage" parameterType="Storage">
        INSERT INTO collection (uid,fid)
        VALUES (#{uid},#{fid})
    </insert>
</mapper>